const quiz = [
  {
    question: "What is the capital of France?",
    options: ["Berlin", "Madrid", "Paris", "Rome"],
    correctAnswer: "Paris"
  },
];


function buildQuiz() {
    // loop through the array of quiz
    // as you loop each item of the array
    // creating a heading element for your question
    // loop through the "options", then create
}

function showResults() {
    let score = 0;

    
}